# What the test suite needs on top of a service's own requirements.
#
# pytest-asyncio was installed on the maintainer's machine but written down
# nowhere, so a fresh checkout silently failed 42 async tests with "async def
# functions are not natively supported" instead of running them. Every async
# test carries @pytest.mark.asyncio, so the default strict mode is correct and
# no asyncio_mode setting is needed.
pytest==9.0.3
pytest-asyncio==1.3.0
