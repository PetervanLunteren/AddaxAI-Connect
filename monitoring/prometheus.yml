# Prometheus Configuration for AddaxAI Connect

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'addaxai-connect'
    environment: 'development'

# Alertmanager configuration (optional, add later)
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets: ['localhost:9093']

scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Node exporter (system metrics) - will add later
  # - job_name: 'node'
  #   static_configs:
  #     - targets: ['node-exporter:9100']

  # FastAPI backend (will add when implemented)
  # - job_name: 'api'
  #   static_configs:
  #     - targets: ['api:8000']
  #   metrics_path: '/metrics'

  # PostgreSQL exporter (will add later)
  # - job_name: 'postgres'
  #   static_configs:
  #     - targets: ['postgres-exporter:9187']

  # Redis exporter (will add later)
  # - job_name: 'redis'
  #   static_configs:
  #     - targets: ['redis-exporter:9121']
