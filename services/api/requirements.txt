# Shared runtime deps (sqlalchemy, psycopg2, asyncpg, redis, boto3, pydantic,
# pydantic-settings, geoalchemy2, python-json-logger, jinja2) come from the
# shared package; see shared/pyproject.toml. Only service-specific deps here.
# fastapi, fastapi-users, python-multipart, bcrypt, email-validator and the
# pydantic pair in shared/pyproject.toml all move together. fastapi 0.141
# wants pydantic >=2.9, fastapi-users 12 pinned python-multipart to 0.0.6 and
# email-validator to <2.1, and fastapi-users 15 drops passlib for pwdlib,
# which needs bcrypt >=4.1.2. Any one of them alone fails to install or fails
# to boot.
fastapi==0.141.1
uvicorn[standard]==0.52.3
alembic==1.12.1
python-jose[cryptography]==3.4.0
python-multipart==0.0.32
bcrypt==5.0.0
fastapi-users[sqlalchemy]==15.0.5
aiosmtplib==5.1.1
email-validator>=2.3.0,<2.4
slowapi==0.1.10
Pillow==12.3.0
httpx==0.25.2
playwright==1.62.0
openpyxl==3.1.2
geojson==3.3.0
pyshp==2.3.1
tzdata>=2026.3
astral>=3.2
numpy<3.0.0
scipy>=1.10.0,<2.0.0
