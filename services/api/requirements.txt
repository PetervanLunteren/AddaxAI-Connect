# Shared runtime deps (sqlalchemy, psycopg2, asyncpg, redis, boto3, pydantic,
# pydantic-settings, geoalchemy2, python-json-logger, jinja2) come from the
# shared package; see shared/pyproject.toml. Only service-specific deps here.
fastapi==0.141.1
uvicorn[standard]==0.52.3
alembic==1.12.1
python-jose[cryptography]==3.3.0
passlib==1.7.4
# Pinned by fastapi-users 12.1.2, which depends on python-multipart==0.0.6
# exactly. 8 advisories want 0.0.31 and a clean image build refuses it with
# ResolutionImpossible, so it moves only when fastapi-users does. Installing
# it by hand into a running container appears to work; that is pip skipping
# the resolve, not the pin going away. Tried 19 Aug 2026.
python-multipart==0.0.6
bcrypt==3.2.2
fastapi-users[sqlalchemy]==12.1.2
aiosmtplib==5.1.1
email-validator>=1.1.0,<2.1
slowapi==0.1.9
Pillow==12.3.0
httpx==0.25.2
playwright==1.62.0
openpyxl==3.1.2
geojson==3.3.0
pyshp==2.3.1
tzdata>=2024.1
astral>=3.2
numpy<2.0.0
scipy>=1.10.0,<2.0.0
