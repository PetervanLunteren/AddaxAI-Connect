# Shared runtime deps (redis, sqlalchemy, pydantic, etc.) come from the shared
# package (shared/pyproject.toml). Only service-specific deps go here.
# Do not add redis here; leaving it unpinned is what let this image drift to
# redis 8.0.0, whose default 5s socket timeout crashed the queue consumer.
requests==2.31.0
Pillow==12.3.0
